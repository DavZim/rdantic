# Is coercion switched off?

Is coercion switched off?

## Usage

``` r
.strict()
```

## Value

`TRUE` when `rdantic.strict` is on, using the cached value if one
top-level validation is already running.

## Examples

``` r
rdantic:::.strict()
#> [1] FALSE
```
