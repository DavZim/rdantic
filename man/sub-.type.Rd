% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combinators.R
\name{[.type}
\alias{[.type}
\title{Fix a type's length, or add a constraint}
\usage{
\method{[}{type}(x, i)
}
\arguments{
\item{x}{A type.}

\item{i}{A length, or an expression in \code{.}.}
}
\value{
A type.
}
\description{
\code{T[n]} is \code{T} with exactly \code{n} elements. \code{T[expr]}, where \code{expr} mentions
\code{.}, is \code{T} restricted to values for which the predicate holds elementwise.
The two chain in any order, and the source text of the predicate becomes
part of the type's name, so an error states the rule that was broken.
}
\details{
\code{T[1]} is the most useful type in R: most recycling bugs are a vector
arriving where one value was meant.
}
\examples{
int[1](3)
try(int[1](c(3, 4)))

num[. > 0](c(1, 2))
try(num[. > 0](c(1, -2)))

small <- int[1][0 < . & . < 10]
small
small(7)
try(small(70))

chr[1][nchar(.) <= 3]("abc")
}
