% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/s7.R
\name{.s7_fields}
\alias{.s7_fields}
\title{The typed fields of an S7 class}
\usage{
.s7_fields(cls)
}
\arguments{
\item{cls}{An S7 class.}
}
\value{
A named list of types, in declaration order.
}
\description{
A class built by \code{\link[=as_s7_class]{as_s7_class()}} carries the struct it came from, so a
round trip keeps the original field types instead of the erased
\code{class_any} the properties were declared with. Otherwise the property
classes are translated one by one. A property is optional exactly when it
declares a \code{default}, which is the same rule \verb{\%default\%} follows.
}
\details{
A property with a \code{getter} and no \code{setter} is computed, so it is not a
field: nothing can supply it, and requiring it would make every such class
impossible to parse.
}
\examples{
if (requireNamespace("S7", quietly = TRUE))
  names(rdantic:::.s7_fields(
    S7::new_class("Blop", properties = list(n = S7::class_double))
  ))
}
\keyword{internal}
