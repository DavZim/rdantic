% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/problems.R
\name{.reject_reserved}
\alias{.reject_reserved}
\title{Reject names that would collide with generated code's own bindings}
\usage{
.reject_reserved(nms, reserved, what)
}
\arguments{
\item{nms}{The supplied names.}

\item{reserved}{The names that must not be used.}

\item{what}{What kind of name this is, e.g. \code{"argument"} or \code{"field"}.}
}
\value{
Nothing; throws when a reserved name is used.
}
\description{
\code{fn()} and \code{struct()} splice user-supplied names into generated code
alongside a handful of internal bookkeeping variables; a name equal to one
of those would silently shadow it instead of erroring, so this rejects the
collision up front instead.
}
\examples{
rdantic:::.reject_reserved(c("x", "y"), c(".r_"), "argument")
try(rdantic:::.reject_reserved(c(".r_"), c(".r_"), "argument"))
}
\keyword{internal}
