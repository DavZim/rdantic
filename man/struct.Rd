% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/struct.R
\name{struct}
\alias{struct}
\title{Define a record type}
\usage{
struct(
  .name,
  ...,
  .description = NULL,
  .parents = character(),
  .extra = c("ignore", "forbid")
)
}
\arguments{
\item{.name}{The struct's name, as a single string.}

\item{...}{Named fields, one type each.}

\item{.description}{The struct's JSON Schema \code{"description"}, or \code{NULL} to
omit it. Use \code{\link[=desc]{desc()}}/\code{\link{\%doc\%}} for a field's own description instead.}

\item{.parents}{Set by \code{\link[=extend]{extend()}}; not usually written by hand.}

\item{.extra}{\code{"forbid"} rejects unknown keys when parsing a list into this
struct; the default \code{"ignore"} drops them.}
}
\value{
A constructor of class \code{typed_struct}, which is also a type.
}
\description{
Returns a constructor with real formals -- so autocomplete works -- which is
itself a type, so structs nest and can be used anywhere a type can. Instances
are plain named lists with a class, which means they copy on modify like
every other R value.
}
\details{
Where a struct is expected, a plain list is parsed into it. That is what makes
JSON input work with no separate JSON mode.
}
\examples{
Account <- struct("Account",
  id      = int[1][. > 0],
  email   = chr[1][grepl("@", ., fixed = TRUE)],
  credit  = num[1][. >= 0] \%default\% 0,
  closed  = date[1] | NULL
)
Account

a <- Account(id = 1, email = "ada@example.org")
a
a$credit

# every problem at once, each with its path
try(Account(id = 0, email = "nope"))

# fields are checked on assignment, and instances are values
b <- a
b$credit <- 10
c(a$credit, b$credit)
try(a$balance <- 10)

# a plain list is parsed
from_list(Account, list(id = 2, email = "bob@example.org"))
try(from_list(struct("Shut", x = int[1], .extra = "forbid"), list(x = 1, y = 2)))
}
\seealso{
\code{\link[=extend]{extend()}}, \code{\link[=partial]{partial()}}, \code{\link[=list_of]{list_of()}}, \code{\link[=fields]{fields()}}
}
