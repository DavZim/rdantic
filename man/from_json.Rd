% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/serialize.R
\name{from_json}
\alias{from_json}
\title{Parse and validate JSON in one step}
\usage{
from_json(t, txt)
}
\arguments{
\item{t}{A type, or anything \code{\link[=as_type]{as_type()}} accepts.}

\item{txt}{A JSON string, or anything \code{\link[jsonlite:fromJSON]{jsonlite::fromJSON()}} reads.}
}
\value{
The validated value.
}
\description{
JSON arrays come back as R vectors and arrays of objects as data frames,
because the declared types accept those shapes -- there is no separate JSON
mode. Reshaping happens only here and in \code{\link[=from_list]{from_list()}}, never when a value
is handed straight to a type.
}
\examples{
Reading <- struct("Reading", station = chr[1], celsius = num[1], at = datetime[1])
from_json(Reading, '{"station": "KOA", "celsius": 21.5, "at": "2024-05-17T09:30:00"}')

from_json(map_of(int[1]), '{"cpu": 4, "memory": 16}')
from_json(frame(id = int, nm = chr), '[{"id": 1, "nm": "a"}]')

try(from_json(Reading, '{"station": ["a", "b"], "celsius": 21.5, "at": "nope"}'))
}
\seealso{
\code{\link[=to_json]{to_json()}}, \code{\link[=from_list]{from_list()}}, \code{\link[=try_parse]{try_parse()}}
}
