% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/struct.R
\name{.bind_instance}
\alias{.bind_instance}
\title{Wrap validated values as an instance}
\usage{
.bind_instance(spec, vals, spec_env = spec)
}
\arguments{
\item{spec}{A struct spec.}

\item{vals}{The validated values.}

\item{spec_env}{\code{spec}, wrapped in an environment; defaults to \code{spec}
itself so direct calls (e.g. from examples) still work.}
}
\value{
An object of class \code{c(name, parents, "typed_instance")}.
}
\description{
Instances are plain lists: copy-on-modify like every other R value, cheap to
build, and \code{identical()}, \code{saveRDS()} and \code{str()} all behave. The \code{spec}
attribute is an environment, not the spec list itself, so \code{object.size()}
-- which charges environments a small fixed cost instead of recursing into
them -- does not report each instance as if it owned a private copy of the
whole (shared) struct definition.
}
\examples{
rdantic:::.bind_instance(rdantic:::.spec(struct("BindDemo", a = int[1])), list(a = 1L))
}
\keyword{internal}
