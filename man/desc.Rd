% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combinators.R
\name{desc}
\alias{desc}
\alias{\%doc\%}
\title{Attach a description to a type's schema}
\usage{
desc(t, description)

t \%doc\% description
}
\arguments{
\item{t}{A type, or anything \code{\link[=as_type]{as_type()}} accepts.}

\item{description}{A single string.}
}
\value{
A type carrying the description.
}
\description{
The description is what \code{\link[=schema]{schema()}} puts under the JSON Schema
\code{"description"} key -- the pydantic-style docstring for a field. It must be
the outermost wrapper in a chain (applied after \verb{[n]}, \verb{[expr]}, \code{opt()}/
\code{|} and \verb{\%default\%}), the same implicit rule \verb{\%default\%} already has,
because an outer combinator builds its own schema fragment and would
otherwise discard it. A description added this way replaces, rather than
duplicates, one \code{\link[=.refined]{.refined()}} wrote automatically.
}
\details{
A struct's own top-level description is set with \code{struct(.description = )}
instead: \code{desc()} on a whole struct builds a new, unregistered constructor,
so a \code{\link[=ref]{ref()}} to that struct's name would still see the undescribed one.

Multiline descriptions are dedented: blank first and last lines and the
indentation shared by every nonblank line are removed. This makes raw
strings (\code{r"(...)"}) convenient for long prose. Use \code{describe()} to keep
all of a struct's longer descriptions together after its declaration.
}
\examples{
age <- int[1][. > 0] \%doc\% "Age in years."
schema(age)$description

Person <- struct("Person",
  name = chr[1] \%doc\% "Full name of the person.",
  occupation = opt(chr[1]) \%doc\% "Current job title, or null if unknown."
)
schema(Person)$properties$occupation
}
