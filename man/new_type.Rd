% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/type.R
\name{new_type}
\alias{new_type}
\title{Create a type}
\usage{
new_type(name, validate, schema = function() list(), ...)
}
\arguments{
\item{name}{The type's name, used in error messages and in \code{print()}.}

\item{validate}{\verb{function(x, path)} returning the (possibly coerced) value,
or the result of \code{\link[=.bad]{.bad()}}/\code{\link[=.fail]{.fail()}}.}

\item{schema}{\verb{function()} returning the JSON Schema fragment for the type.}

\item{...}{Extra entries stored in the spec, such as \code{scalar}, \code{empty},
\code{default}, \code{elt} or \code{members}.}
}
\value{
An object of class \code{type}.
}
\description{
A type is a closure that validates a value and an attached \code{spec} describing
it. Calling the type validates and returns the value; everything else in
rdantic is built by composing types.
}
\examples{
even <- new_type(
  "even",
  validate = function(x, path) {
    if (is.numeric(x) && all(x \%\% 2 == 0)) x else rdantic:::.bad(path, "even", type_of(x))
  },
  schema = function() list(type = "integer", multipleOf = 2)
)
even(c(2, 4))
try(even(3))
schema(even)
}
