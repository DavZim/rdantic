% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/primitives.R
\name{primitives}
\alias{primitives}
\alias{int}
\alias{num}
\alias{chr}
\alias{lgl}
\alias{anything}
\alias{null_t}
\title{Primitive vector types}
\format{
Objects of class \code{type}.
}
\usage{
int(x)

num(x)

chr(x)

lgl(x)

anything(x)

null_t(x)
}
\arguments{
\item{x}{The value to validate.}
}
\description{
The four atomic vector types, plus \code{anything} and the null type. Each is an
ordinary value: call it to validate, subset it with \code{[} to fix a length or
add a constraint, combine it with \code{|}.
}
\details{
Coercion is lossless only. A whole double becomes an integer and an integer
becomes a double; \code{1.5} never becomes \code{1L}, \code{"1"} never becomes \code{1}, and a
classed atomic such as a \code{Date} is never quietly unclassed. Set
\code{options(rdantic.strict = TRUE)} to switch coercion off entirely.
}
\examples{
int(c(1, 2, 3))   # whole doubles become integers
num(2L)           # integers become doubles
chr(factor("a"))  # factors become characters
lgl(c(TRUE, NA))
anything(list(1, "a"))

try(int(1.5))     # lossy
try(int("3"))     # strings are never parsed
try(int(1e10))    # outside integer range

int[1]            # exactly one integer
num[. > 0]        # positive doubles
}
\seealso{
\link{date}, \code{\link[=fct]{fct()}}, \code{\link[=type_from]{type_from()}}
}
