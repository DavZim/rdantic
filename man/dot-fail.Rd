% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/problems.R
\name{.fail}
\alias{.fail}
\title{Report a validation failure}
\usage{
.fail(problems)
}
\arguments{
\item{problems}{A list of \verb{\{path, expected, got, hint\}} records.}
}
\value{
A list of problem records, of class \code{typed_problems}.
}
\description{
A validator returns the (possibly coerced) value on success, or the object
built here on failure, so the happy path allocates nothing beyond the value
itself. \code{\link[=.is_bad]{.is_bad()}} tells the two apart.
}
\examples{
p <- rdantic:::.fail(list(list(path = "$x", expected = "int", got = "chr[1]")))
rdantic:::.is_bad(p)
}
\keyword{internal}
