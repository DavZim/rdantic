% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/type.R
\name{type_from}
\alias{type_from}
\title{Build a type from a predicate}
\usage{
type_from(name, test, coerce = NULL, json = "string", scalar = FALSE)
}
\arguments{
\item{name}{The type's name, shown in errors.}

\item{test}{\verb{function(x)} returning \code{TRUE} when \code{x} is acceptable.}

\item{coerce}{Optional \verb{function(x)} returning a converted value, tried only
when \code{test} fails and \code{rdantic.strict} is off.}

\item{json}{The JSON Schema type name.}

\item{scalar}{Whether a length-1 value should serialise as a JSON scalar
rather than a one-element array.}
}
\value{
A type.
}
\description{
The escape hatch for anything the built-in types do not cover, without
touching rdantic's internal problem format.
}
\examples{
hex <- type_from("hex", function(x) is.character(x) && all(grepl("^#[0-9a-f]{6}$", x)))
hex("#00ff99")
try(hex("green"))

port <- type_from(
  "port",
  function(x) is.integer(x) && length(x) == 1 && x > 0 && x < 65536,
  coerce = function(x) as.integer(x),
  json = "integer",
  scalar = TRUE
)
port(8080)
}
