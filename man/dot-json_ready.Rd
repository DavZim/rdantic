% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/serialize.R
\name{.json_ready}
\alias{.json_ready}
\title{Walk a value alongside its declared type}
\usage{
.json_ready(x, t)
}
\arguments{
\item{x}{The value.}

\item{t}{Its declared type, or \code{NULL} when unknown.}
}
\value{
A structure ready for \code{\link[jsonlite:toJSON]{jsonlite::toJSON()}}.
}
\description{
A length-1 vector is only unboxed to a JSON scalar when its type says \verb{[1]}
(or \code{one_of}); otherwise \code{I()} keeps it an array, so a \code{tags = "a"} field
declared \code{chr} serialises as \verb{["a"]}, not \code{"a"}.
}
\examples{
rdantic:::.json_ready("a", chr)
rdantic:::.json_ready("a", chr[1])
}
\keyword{internal}
