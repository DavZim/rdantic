% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/serialize.R
\name{to_json}
\alias{to_json}
\title{Serialise to JSON, guided by the declared types}
\usage{
to_json(x, pretty = TRUE, ...)
}
\arguments{
\item{x}{An instance, a schema, or any value jsonlite can handle.}

\item{pretty}{Whether to indent the output.}

\item{...}{Passed to \code{\link[jsonlite:toJSON]{jsonlite::toJSON()}}.}
}
\value{
A \code{json} string.
}
\description{
A field declared \verb{[1]} becomes a JSON scalar; any other vector becomes an
array, even with one element. That is the difference R's own types cannot
express and JSON needs.
}
\examples{
Tagged <- struct("Tagged", id = int[1], tags = chr, seen = date[1] | NULL)
to_json(Tagged(id = 3, tags = "cobol"))
to_json(Tagged(id = 3, tags = c("a", "b"), seen = as.Date("2024-01-01")))
to_json(schema(Tagged))
}
\seealso{
\code{\link[=from_json]{from_json()}}, \code{\link[=schema]{schema()}}
}
