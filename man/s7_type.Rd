% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/s7.R
\name{s7_type}
\alias{s7_type}
\title{Use an S7 class as a type}
\usage{
s7_type(cls)
}
\arguments{
\item{cls}{An S7 class, as returned by \code{S7::new_class()}.}
}
\value{
A type.
}
\description{
The class becomes an ordinary rdantic type: it can be a struct field, an
argument of \code{\link[=fn]{fn()}}, the element type of \code{\link[=list_of]{list_of()}}, and it parses a plain
list -- so \code{\link[=from_json]{from_json()}} builds real S7 objects, property by property, with
every problem reported under its path.
}
\details{
Property classes are translated where they carry over (\code{class_integer},
\code{class_double}, \code{class_character}, \code{class_logical}, \code{class_list},
\code{class_Date}, \code{class_POSIXct}, unconstrained \code{class_data.frame} values,
unions, and nested S7 classes); anything else validates as \link{anything}. A
property is required unless it declares a \code{default}. Expression-valued
defaults remain S7's responsibility and are evaluated for each instance.
Computed properties (a getter without a setter) are not input fields.

\code{\link[=as_type]{as_type()}} calls this for you, so an S7 class can be written directly
wherever a type is expected.
}
\examples{
if (requireNamespace("S7", quietly = TRUE)) {
  library(S7)   # for `@`, which S7 supplies on R < 4.3

  Pin <- new_class("Pin", properties = list(
    lat = class_double,
    lon = class_double
  ))

  s7_type(Pin)
  schema(Pin)

  # an S7 class used as a struct field
  Trip <- struct("Trip", label = chr[1], start = Pin)
  t <- from_json(Trip, '{"label": "home", "start": {"lat": 1, "lon": 2}}')
  t$start

  try(from_list(Trip, list(label = "home", start = list(lat = "x", lon = 2))))
}
}
\seealso{
\code{\link[=as_s7_class]{as_s7_class()}} for the other direction.
}
