% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/containers.R
\name{map_of}
\alias{map_of}
\title{Map types}
\usage{
map_of(t)
}
\arguments{
\item{t}{The value type.}
}
\value{
A type.
}
\description{
The map counterpart of \code{list_of(T)}: arbitrary keys, one value type, kept
by name -- and what a JSON object with arbitrary keys parses into. A
failure is reported by key rather than by position.
}
\details{
Reach for \code{list_of(a = T, b = T)} instead when the key set is fixed and
known up front.
}
\examples{
budget <- map_of(int[1][. > 0])
budget(list(cpu = 4, memory = 16))
try(budget(list(cpu = 4, memory = 0)))
try(map_of(int[1])(list(1, 2)))         # unnamed input is list_of(), not this

schema(budget)
from_json(map_of(int[1]), '{"cpu": 4, "memory": 16}')
}
\seealso{
\code{\link[=list_of]{list_of()}} for an array, or a fixed key set.
}
