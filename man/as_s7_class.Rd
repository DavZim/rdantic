% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/s7.R
\name{as_s7_class}
\alias{as_s7_class}
\title{Turn a struct into an S7 class}
\usage{
as_s7_class(x, .name = NULL)
}
\arguments{
\item{x}{A struct, or anything \code{\link[=as_type]{as_type()}} accepts that resolves to one.}

\item{.name}{The class name; defaults to the struct's.}
}
\value{
An S7 class.
}
\description{
The properties are the struct's fields and every one of them validates
through its rdantic type on construction and on \verb{@<-}, coercing where
rdantic would coerce and raising the same \code{typed_error}. Use it to hand an
rdantic-checked record to code that expects S7 -- \code{S7::prop()}, \code{@}, and
S7 generics all work on the result.
}
\details{
Properties are declared \code{S7::class_any}, because the real constraint is the
rdantic type, not S7's class check. A struct made with \code{\link[=extend]{extend()}} becomes
an S7 subclass of its parent's class; if it retypes one of the parent's
fields the hierarchy is flattened with a warning, because S7 keeps the
parent's property definition and the narrower type would be ignored.

Reach for \code{\link[=s7_struct]{s7_struct()}} instead when there is no struct to convert.
}
\examples{
if (requireNamespace("S7", quietly = TRUE)) {
  library(S7)   # for `@`, which S7 supplies on R < 4.3

  Account <- struct("Account",
    id     = int[1][. > 0],
    email  = chr[1][grepl("@", ., fixed = TRUE)],
    credit = num[1][. >= 0] \%default\% 0
  )
  S7Account <- as_s7_class(Account)

  a <- S7Account(id = 1, email = "ada@example.org")
  a@credit

  a@credit <- 10L   # coerced to double, exactly as rdantic would
  typeof(a@credit)

  try(a@id <- 0)
  try(S7Account(id = 1))
}
}
\seealso{
\code{\link[=s7_struct]{s7_struct()}}, \code{\link[=s7_type]{s7_type()}} for the other direction.
}
