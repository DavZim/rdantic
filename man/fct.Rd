% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temporal.R
\name{fct}
\alias{fct}
\title{Factor type}
\usage{
fct(...)
}
\arguments{
\item{...}{The levels, in order.}
}
\value{
A type.
}
\description{
\code{fct(a, b, ...)} accepts a factor with exactly those levels, and promotes
character input whose values are all among them. \code{fct()} with no arguments
accepts any factor and never coerces.
}
\details{
Use \code{\link[=one_of]{one_of()}} when you want a plain string out, and \code{fct()} when you want a
factor with a fixed, ordered level set.
}
\examples{
size <- fct("low", "mid", "high")
size("mid")
levels(size(c("high", "low")))
try(size("enormous"))

fct()(factor("anything"))
schema(size)
}
\seealso{
\code{\link[=one_of]{one_of()}}, \link{primitives}
}
