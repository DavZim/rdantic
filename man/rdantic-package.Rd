% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rdantic-package.R
\docType{package}
\name{rdantic-package}
\alias{rdantic}
\alias{rdantic-package}
\title{rdantic: declared types, checked at the boundary}
\description{
A proof-of-concept port of pydantic's central idea to R: declare what a value
is, once, and have it checked where it enters your code, with coercion only
where nothing can be lost and a complete report where it cannot.
}
\details{
Types are ordinary values built from operators R already has:
\itemize{
\item \link{primitives} -- \code{int}, \code{num}, \code{chr}, \code{lgl}, \code{anything}
\item \link{datetimes} and \code{\link[=fct]{fct()}} -- \code{date}, \code{datetime}, factors
\item \code{T[n]} and \code{T[. > 0]} -- length and constraints, see [\verb{[.type}]
\item \code{A | B}, \code{\link[=opt]{opt()}}, \code{\link[=no_na]{no_na()}}, \code{\link[=one_of]{one_of()}}, \verb{\%default\%}
\item \code{\link[=list_of]{list_of()}}, \code{\link[=map_of]{map_of()}} and \code{\link[=frame]{frame()}} -- lists, maps and data frames
\item \code{\link[=struct]{struct()}}, \code{\link[=extend]{extend()}}, \code{\link[=partial]{partial()}} -- records
\item \code{\link[=fn]{fn()}} -- typed functions
\item \code{\link[=parse_as]{parse_as()}}, \code{\link[=try_parse]{try_parse()}}, \code{\link[=from_json]{from_json()}}, \code{\link[=to_json]{to_json()}}, \code{\link[=schema]{schema()}}
}
}
\section{Options}{

\code{rdantic.strict} switches coercion off, so a value must already be the right
R type. \code{rdantic.check} set to \code{FALSE} skips the checks in \code{\link[=fn]{fn()}}.
}

\examples{
Order <- struct("Order",
  id         = int[1][. > 0],
  qty        = int[1][. > 0],
  unit_price = num[1][. >= 0],
  coupon     = num[1][0 <= . & . <= 1] \%default\% 0
)

Order(id = 1042, qty = 3, unit_price = 19.99)
try(Order(id = "1042", qty = 3, unit_price = 19.99, coupon = 1.2))

net <- fn(o = Order, ~ num[1][. >= 0], {
  o$qty * o$unit_price * (1 - o$coupon)
})
net(Order(id = 1, qty = 3, unit_price = 19.99))
}
\author{
\strong{Maintainer}: David Zimmermann-Kollenda \email{david_j_zimmermann@hotmail.com}

Authors:
\itemize{
  \item David Zimmermann-Kollenda \email{david_j_zimmermann@hotmail.com}
}

}
\keyword{internal}
